import { createServiceDecorator } from "@common/ioc";

import { EntityHolder } from "@common/store/holders";

import { I{{properCase name}}, I{{properCase name}}Request } from "@api";

export const I{{properCase name}}DataStore = createServiceDecorator<I{{properCase name}}DataStore>();

export interface I{{properCase name}}DataStore {
  holder: EntityHolder<I{{properCase name}}, I{{properCase name}}Request>;

  data: I{{properCase name}} | null;
  error?: string;
  isLoading: boolean;
  isReady: boolean;

  load(params?: I{{properCase name}}Request): Promise<void>;
}
